import "./HbAuthMock";
import { HbApp } from "../HbApp";
/**
 * This file imports all mocks for running without
 * the need for firebase or real implementations.
 * Used primarily for the storybook components.
 */
HbApp.init();
